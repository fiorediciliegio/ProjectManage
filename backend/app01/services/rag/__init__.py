"""RAG service submodules.

The public entrypoints are still exported from ``langchain_rag_service`` so
existing views, tasks, and tests can keep their imports stable while the
implementation is split by responsibility.
"""
